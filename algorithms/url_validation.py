# These are function entry points to match the URL validation 
# options
def all(urlList):
	# This method is added for completeness in mapping options to a
	# function, but functionally, this function does not perform 
	# any real work at this time.
	return
	
def valid(urlList):
	print "NotImplemented! - Return only valid URLs?"
	return
	
def invalid(urlList):
	print "NotImplemented! - Return only invalid URLs?"
	return
